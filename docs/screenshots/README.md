# Screenshots

Two PNGs are referenced from the project README:

- `main.png` — main Streamlit page: 4 critic tabs + cross-challenge + supervisor verdict
- `ablation.png` — Ablation Results tab: 4 metric cards + comparison table + bar charts

The captures themselves are not in source control yet — they're created by:

```bash
streamlit run src/ui/streamlit_app.py
```

…then taking screenshots of the two views and saving them here.
